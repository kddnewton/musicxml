#!/usr/bin/env ruby

require 'bundler'
Bundler.require

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'musicxml'

$score = MusicXML::Parser.new(ARGV.shift).parse

require 'irb'
IRB.start
